# 绑定网卡地址
serviceAddress: "localhost"
# 绑定端口
servicePort: 8123

# 线程池数量，当服务器玩家变多的时候稍微提高这个值可以优化消息延迟
# 但是设置得太高反而会过分地消耗服务器资源
# 线程池为 3 的时候仍然可以撑起一个日常在线500~1000人的服务器（生存都市亲测）
# 一般情况下，推荐将这个值设置为 CPU 核心数（或 CPU 核心数 * 2）
nioThread: 12

# 允许连接至服务的ip名单
acceptList:
  - "127.0.0.1"
