# 绑定网卡地址
serviceAddress: "localhost"
# 绑定端口
servicePort: 8123

# 线程池数量，当服务器玩家变多的时候稍微提高这个值可以优化消息延迟
# 但是设置得太高反而会过分地消耗服务器资源（不建议超过20）
# 线程池为 3 的时候仍然可以撑起一个日常在线300人左右的服务器（生存都市亲测）
# 一般情况下，有以下推荐值
# 推荐值1: CPU核心数 ~ CPU核心数*2
# 推荐值2： 子服务端数量 / 2
nioThread: 6

# 允许连接至服务的 ip 名单
acceptList:
  - "127.0.0.1"
